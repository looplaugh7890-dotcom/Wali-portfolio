Drop Wali's real portrait here as `portrait.jpg` (or .png), then update
components/Hero.tsx to render it with next/image instead of the current
placeholder panel. Recommended: a vertical (portrait-orientation) photo,
at least 1000x1250px, well-lit, plain or softly blurred background so it
reads clearly against the dark hero.
